const prompt = require("prompt-sync")() //adiciona os pacotes na entrada dos dados

const num1 = Number(prompt("1º Número: "));
const num2 = Number(prompt("2º número: "));
const soma = num1 + num2;
console.log("A soma dos números é: "  + soma);

